// npm init -y   for generating json files so that you can install dependencies
// npm install fetch-json-file   for installing dependencies
// npm install -g json-server   for installing global dependencies
// npm install -g nodemon   for installing global dependencies
// npm install -g json-server   for installing global dependencies
