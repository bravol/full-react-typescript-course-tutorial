let myName: string;
let meaningOfLife: number;
let isLoaidng: boolean;

let album: any;

let albums: string | number; //this is a union type

isLoaidng = true;

album = 2;
//adding numbers
const sum = (a: number, b: number) => {
  return a + b;
};

let postId: string | number;
let isActive: number | boolean;

let re: RegExp = /^[a-zA-Z0-9]+$/;
